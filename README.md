# cyp-pkg-lang-java-jdk25

Eclipse Temurin OpenJDK 25 的 cyp 包仓库。

## 触发构建

在 GitHub Actions 页面手动触发 `Build JDK Package`，输入版本号。

## 支持平台

- macOS ARM64 (Apple Silicon)
- macOS Intel (x86_64)
- Linux x86_64
- Linux ARM64
- Windows x86_64

## 安装

```bash
cyp install cc.cyteam.lang.java.jdk25
```
