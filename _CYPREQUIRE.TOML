# JDK 运行时 - 本包即提供 java 运行时，无需额外依赖
[[dependency]]
name = "unzip"
type = "system"
check_command = "unzip -v >/dev/null 2>&1"
install_hint = "安装 unzip: macOS: brew install unzip | Linux: apt install unzip"
